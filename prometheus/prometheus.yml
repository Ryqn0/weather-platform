# prometheus/prometheus.yml

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'weather-consumer'
    static_configs:
      - targets: ['consumer:8000']

  - job_name: 'weather-alerts'
    static_configs:
      - targets: ['alerts:8000']
